using Avalonia.Controls;

namespace ReyEngine.App.Views;

/// <summary>M697: Tools ▸ Add prop to map… - places a character the map's package already carries.
/// Everything it does lives in <see cref="ViewModels.AddPropViewModel"/>.</summary>
public partial class AddPropWindow : Window
{
    public AddPropWindow()
    {
        InitializeComponent();
    }
}
