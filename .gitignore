## .NET / Visual Studio
bin/
obj/
*.user
*.suo
.vs/
.idea/
*.DotSettings.user

## ReyEngine — downloaded hash dictionaries & cache (large, regenerated by Sync Hashes)
/data/hashes/*.txt
/data/hashes/*.txt.*
/data/hashes/*.cache
/data/hashes/communitydragon/
# M367: LeagueToolkit meta-class dump. Fetched, not vendored - it ships no licence and is
# re-dumped every patch, so a committed copy would be both a redistribution and stale.
/data/meta/

## Keep dictionary folder docs
!/data/hashes/README.md
publish/

## Codex scratch probes (1.5 GB of build output) - never part of the repo
.codex_tmp/
